import * as stylex from '@stylexjs/stylex'

export const styles = stylex.create({
  wrapper: {
    width: '100%',
    position: 'relative',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
})
