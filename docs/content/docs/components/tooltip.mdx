---
title: Tooltip
description: Documentation for the Tooltip component.
---

import { Tooltip, TooltipTrigger, Button } from '@moul-dev/ui';


# Tooltip

## Preview

<ComponentPlayground component="Tooltip" />

The `Tooltip` component is built with React Aria and styled using StyleX.

## Import

```tsx
import { Tooltip } from '@moul-dev/ui';
```

## Usage

Here is a basic example of how to use the `Tooltip` component:

```tsx
import { Tooltip } from '@moul-dev/ui';

export default function Example() {
  return (
    <Tooltip>
      {/* Component content */}
    </Tooltip>
  );
}
```

## Props

### Tooltip Props

<TypeTable
  type={{
    'placement': {
      type: "string",
      default: "'top'",
      description: "The placement of the tooltip relative to its trigger.",
    },
    'isOpen': {
      type: "boolean",
      description: "Whether the tooltip is open (controlled).",
    },
    'defaultOpen': {
      type: "boolean",
      description: "Whether the tooltip is initially open (uncontrolled).",
    },
  }}
/>

<Accordions>
  <Accordion title="Value">
    <TypeTable
          type={{
            'onOpenChange': {
              type: "(isOpen: boolean) => void",
              description: "Handler called when open state changes.",
            },
          }}
        />
  </Accordion>
  <Accordion title="Styling">
    <TypeTable
          type={{
            'className': {
              type: "ClassNameOrFunction<TooltipRenderProps>",
              default: "'react-aria-Tooltip'",
              description: "The CSS className.",
            },
          }}
        />
  </Accordion>
</Accordions>

Default className: `react-aria-Tooltip`

| Render Prop | CSS Selector |
| :--- | :--- |
| `isOpen` <br/> Whether the tooltip is currently open. | `[data-open]` |
| `placement` <br/> The placement of the tooltip. | `[data-placement]` |

