---
title: ErrorMessage
description: Documentation for the ErrorMessage component.
---

import { ErrorMessage } from '@moul-dev/ui';


# ErrorMessage

## Preview

<ComponentPreview>
  <ErrorMessage>Invalid email address format.</ErrorMessage>
</ComponentPreview>

The `ErrorMessage` component is built with React Aria and styled using StyleX.

## Import

```tsx
import { ErrorMessage } from '@moul-dev/ui';
```

## Usage

Here is a basic example of how to use the `ErrorMessage` component:

```tsx
import { ErrorMessage } from '@moul-dev/ui';

export default function Example() {
  return (
    <ErrorMessage>
      {/* Component content */}
    </ErrorMessage>
  );
}
```
