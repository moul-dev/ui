---
title: Checkbox
description: Documentation for the Checkbox component.
reactAria: Checkbox
---

import { Checkbox } from '@moul-dev/ui';


# Checkbox

## Preview

<ComponentPreview>
  <div className="flex flex-col gap-2">
    <Checkbox>Unchecked</Checkbox>
    <Checkbox defaultSelected>Checked</Checkbox>
    <Checkbox isDisabled>Disabled</Checkbox>
  </div>
</ComponentPreview>

The `Checkbox` component is built with React Aria and styled using StyleX.

## Import

```tsx
import { Checkbox } from '@moul-dev/ui';
```

## Usage

Here is a basic example of how to use the `Checkbox` component:

```tsx
import { Checkbox } from '@moul-dev/ui';

export default function Example() {
  return (
    <Checkbox>
      Checkbox Label
    </Checkbox>
  );
}
```

## Variants

The `Checkbox` component supports three visual variants: `primary` (default), `secondary`, and `tertiary`.

<ComponentPreview>
  <div className="flex flex-col gap-4">
    <div className="flex flex-col gap-2">
      <span className="text-sm font-medium text-neutral-500">Primary (default)</span>
      <div className="flex gap-4">
        <Checkbox variant="primary">Unchecked</Checkbox>
        <Checkbox variant="primary" defaultSelected>Checked</Checkbox>
      </div>
    </div>
    <div className="flex flex-col gap-2">
      <span className="text-sm font-medium text-neutral-500">Secondary</span>
      <div className="flex gap-4">
        <Checkbox variant="secondary">Unchecked</Checkbox>
        <Checkbox variant="secondary" defaultSelected>Checked</Checkbox>
      </div>
    </div>
    <div className="flex flex-col gap-2">
      <span className="text-sm font-medium text-neutral-500">Tertiary</span>
      <div className="flex gap-4">
        <Checkbox variant="tertiary">Unchecked</Checkbox>
        <Checkbox variant="tertiary" defaultSelected>Checked</Checkbox>
      </div>
    </div>
  </div>
</ComponentPreview>

### Primary Variant
The primary variant uses the primary accent brand colors, suited for main user actions.
```tsx
<Checkbox variant="primary" defaultSelected>Primary</Checkbox>
```

### Secondary Variant
The secondary variant uses monochrome neutral styling, providing a clean alternative for lists and tables.
```tsx
<Checkbox variant="secondary" defaultSelected>Secondary</Checkbox>
```

### Tertiary Variant
The tertiary variant is a hollow outline checkbox style that stays transparent and applies primary colors to the border and checkmark itself.
```tsx
<Checkbox variant="tertiary" defaultSelected>Tertiary</Checkbox>
```

## Props

### Checkbox Props

<TypeTable
  type={{
    'variant': {
      type: "'primary' | 'secondary' | 'tertiary'",
      default: "'primary'",
      description: "The visual style variant of the checkbox.",
    },
    'isSelected': {
      type: "boolean",
      description: "Whether the checkbox is checked (controlled).",
    },
    'defaultSelected': {
      type: "boolean",
      description: "Whether the checkbox is checked by default (uncontrolled).",
    },
    'isIndeterminate': {
      type: "boolean",
      description: "Whether the checkbox is indeterminate.",
    },
    'isDisabled': {
      type: "boolean",
      default: "false",
      description: "Whether the checkbox is disabled.",
    },
  }}
/>

<Accordions>
  <Accordion title="Value">
    <TypeTable
          type={{
            'onChange': {
              type: "(isSelected: boolean) => void",
              description: "Handler called when the checked state changes.",
            },
          }}
        />
  </Accordion>
  <Accordion title="Styling">
    <TypeTable
          type={{
            'className': {
              type: "ClassNameOrFunction<CheckboxRenderProps>",
              default: "'react-aria-Checkbox'",
              description: "The CSS className for the element.",
            },
          }}
        />
  </Accordion>
</Accordions>

Default className: `react-aria-Checkbox`

| Render Prop | CSS Selector |
| :--- | :--- |
| `isSelected` <br/> Whether the checkbox is selected. | `[data-selected]` |
| `isIndeterminate` <br/> Whether the checkbox is indeterminate. | `[data-indeterminate]` |
| `isHovered` <br/> Whether the checkbox is hovered. | `[data-hovered]` |
| `isPressed` <br/> Whether the checkbox is pressed. | `[data-pressed]` |
| `isFocused` <br/> Whether the checkbox is focused. | `[data-focused]` |
| `isFocusVisible` <br/> Whether the checkbox is keyboard focused. | `[data-focus-visible]` |
| `isDisabled` <br/> Whether the checkbox is disabled. | `[data-disabled]` |

