---
title: AlertDialog
description: Documentation for the AlertDialog component.
reactAria: Dialog
---

import { AlertDialog } from '@moul-dev/ui';


# AlertDialog

## Preview

<ComponentPreview>
  <AlertDialogDemo />
</ComponentPreview>

The `AlertDialog` component is built with React Aria and styled using StyleX.

## Import

```tsx
import { AlertDialog } from '@moul-dev/ui';
```

## Usage

Here is a basic example of how to use the `AlertDialog` component:

```tsx
import {
  AlertDialog,
  AlertDialogHeader,
  AlertDialogBody,
  AlertDialogFooter,
  Button
} from '@moul-dev/ui';

export default function Example() {
  return (
    <AlertDialog>
      <AlertDialogHeader>Deactivate Account</AlertDialogHeader>
      <AlertDialogBody>
        Are you sure you want to deactivate your account? This action cannot be undone.
      </AlertDialogBody>
      <AlertDialogFooter>
        <Button variant="outline" onPress={() => {}}>Cancel</Button>
        <Button variant="danger" onPress={() => {}}>Confirm</Button>
      </AlertDialogFooter>
    </AlertDialog>
  );
}
```

## Props

### AlertDialog Props

<TypeTable
  type={{
    'role': {
      type: "'dialog' | 'alertdialog'",
      default: "'alertdialog'",
      description: "The accessibility role matching the content dialog behavior.",
    },
  }}
/>

<Accordions>
  <Accordion title="Styling">
    <TypeTable
          type={{
            'className': {
              type: "string",
              description: "The CSS className.",
            },
          }}
        />
  </Accordion>
</Accordions>

Default className: `react-aria-Dialog`

