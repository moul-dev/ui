---
title: Surface
description: Documentation for the Surface component.
---

import { Surface } from '@moul-dev/ui';


# Surface

## Preview

<ComponentPreview>
  <div className="flex gap-6">
    <Surface elevation={1} className="p-6 rounded-lg w-24 h-24 flex items-center justify-center">Flat</Surface>
    <Surface elevation={2} className="p-6 rounded-lg w-24 h-24 flex items-center justify-center">Raised</Surface>
    <Surface elevation={3} className="p-6 rounded-lg w-24 h-24 flex items-center justify-center">Overlay</Surface>
  </div>
</ComponentPreview>

The `Surface` component is built with React Aria and styled using StyleX.

## Import

```tsx
import { Surface } from '@moul-dev/ui';
```

## Usage

Here is a basic example of how to use the `Surface` component:

```tsx
import { Surface } from '@moul-dev/ui';

export default function Example() {
  return (
    <Surface>
      {/* Component content */}
    </Surface>
  );
}
```
