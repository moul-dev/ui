---
title: SearchField
description: Documentation for the SearchField component.
---

import { SearchField } from '@moul-dev/ui';


# SearchField

## Preview

<ComponentPreview>
  <SearchField label="Search" placeholder="Type to search..." />
</ComponentPreview>

The `SearchField` component is built with React Aria and styled using StyleX.

## Import

```tsx
import { SearchField } from '@moul-dev/ui';
```

## Usage

Here is a basic example of how to use the `SearchField` component:

```tsx
import { SearchField } from '@moul-dev/ui';

export default function Example() {
  return (
    <SearchField>
      {/* Component content */}
    </SearchField>
  );
}
```

## Props

### SearchField Props

<TypeTable
  type={{
    'variant': {
      type: "'primary' | 'secondary'",
      default: "'primary'",
      description: "The visual style variant of the search field.",
    },
    'value': {
      type: "string",
      description: "The current value (controlled).",
    },
    'defaultValue': {
      type: "string",
      description: "The default value (uncontrolled).",
    },
    'placeholder': {
      type: "string",
      description: "Placeholder text inside the input.",
    },
    'isDisabled': {
      type: "boolean",
      default: "false",
      description: "Whether the field is disabled.",
    },
  }}
/>

<Accordions>
  <Accordion title="Value">
    <TypeTable
          type={{
            'onChange': {
              type: "(value: string) => void",
              description: "Handler called when the value changes.",
            },
            'onSubmit': {
              type: "(value: string) => void",
              description: "Handler called when the user submits the search query.",
            },
            'onClear': {
              type: "() => void",
              description: "Handler called when the user clears the search field.",
            },
          }}
        />
  </Accordion>
  <Accordion title="Styling">
    <TypeTable
          type={{
            'className': {
              type: "ClassNameOrFunction<SearchFieldRenderProps>",
              default: "'react-aria-SearchField'",
              description: "The CSS className.",
            },
          }}
        />
  </Accordion>
</Accordions>

Default className: `react-aria-SearchField`

| Render Prop | CSS Selector |
| :--- | :--- |
| `isDisabled` <br/> Whether the search field is disabled. | `[data-disabled]` |
| `isInvalid` <br/> Whether the search field is invalid. | `[data-invalid]` |

