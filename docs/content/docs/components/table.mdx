---
title: Table
description: Zero-runtime semantic HTML data table primitives styled with StyleX, tailored for TanStack Table, TanStack Virtual, TanStack Query, TanStack Store, and TanStack Pacer.
---

import { Table, TableHeader, TableBody, TableFooter, TableRow, TableHead, TableCell, TableCaption, TableEmpty, TableSkeleton, Badge, Button } from '@moul-dev/ui';

# Table

The `Table` component is a collection of composable, zero-runtime semantic HTML table primitives styled with **StyleX**. Built to pair seamlessly with headless data engines like **TanStack Table**, it provides accessible table markup, customizable visual variants (`dense`, `striped`, `hoverable`, `stickyHeader`), column pinning, sort indicators, numeric alignment, and built-in loading and empty states.

## Preview

<ComponentPlayground component="Table" />

## Import

```tsx
import {
  Table,
  TableHeader,
  TableBody,
  TableFooter,
  TableRow,
  TableHead,
  TableCell,
  TableCaption,
  TableEmpty,
  TableSkeleton,
} from '@moul-dev/ui';
```

---

## Interactive TanStack Table Showcase

Experience a full-featured data table powered by **TanStack Table**, **TanStack Store** (reactive view preferences), and **TanStack Pacer** (debounced search filtering):

<ComponentPreview>
  <TableDemo />
</ComponentPreview>

---

## Basic Semantic Usage

Construct a table declaratively with standard semantic primitives without any external state dependencies:

<ComponentPreview>
  <Table aria-label="Cluster Services Table" className="w-full max-w-2xl">
    <TableCaption>Active production services across clusters</TableCaption>
    <TableHeader>
      <TableRow>
        <TableHead>Service Name</TableHead>
        <TableHead>Region</TableHead>
        <TableHead>Status</TableHead>
        <TableHead align="numeric">Throughput</TableHead>
      </TableRow>
    </TableHeader>
    <TableBody>
      <TableRow>
        <TableCell className="font-semibold">Auth Gateway</TableCell>
        <TableCell>us-east-1</TableCell>
        <TableCell><Badge variant="success" dot>Operational</Badge></TableCell>
        <TableCell align="numeric">1.2M req/s</TableCell>
      </TableRow>
      <TableRow>
        <TableCell className="font-semibold">PostgreSQL Primary</TableCell>
        <TableCell>us-west-2</TableCell>
        <TableCell><Badge variant="success" dot>Operational</Badge></TableCell>
        <TableCell align="numeric">450k req/s</TableCell>
      </TableRow>
      <TableRow>
        <TableCell className="font-semibold">Edge CDN Gateway</TableCell>
        <TableCell>global</TableCell>
        <TableCell><Badge variant="warning" dot>Degraded</Badge></TableCell>
        <TableCell align="numeric">3.8M req/s</TableCell>
      </TableRow>
    </TableBody>
    <TableFooter>
      <TableRow>
        <TableCell colSpan={3}>Cluster Summary</TableCell>
        <TableCell align="numeric">3 Nodes Active</TableCell>
      </TableRow>
    </TableFooter>
  </Table>
</ComponentPreview>

```tsx
import {
  Table,
  TableHeader,
  TableBody,
  TableFooter,
  TableRow,
  TableHead,
  TableCell,
  TableCaption,
  Badge,
} from '@moul-dev/ui';

export default function BasicTableExample() {
  return (
    <Table aria-label="Cluster Services Table">
      <TableCaption>Active production services across clusters</TableCaption>
      <TableHeader>
        <TableRow>
          <TableHead>Service Name</TableHead>
          <TableHead>Region</TableHead>
          <TableHead>Status</TableHead>
          <TableHead align="numeric">Throughput</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        <TableRow>
          <TableCell className="font-semibold">Auth Gateway</TableCell>
          <TableCell>us-east-1</TableCell>
          <TableCell><Badge variant="success" size="sm" dot>Operational</Badge></TableCell>
          <TableCell align="numeric">1.2M req/s</TableCell>
        </TableRow>
        <TableRow>
          <TableCell className="font-semibold">PostgreSQL Primary</TableCell>
          <TableCell>us-west-2</TableCell>
          <TableCell><Badge variant="success" size="sm" dot>Operational</Badge></TableCell>
          <TableCell align="numeric">450k req/s</TableCell>
        </TableRow>
        <TableRow>
          <TableCell className="font-semibold">Edge CDN Gateway</TableCell>
          <TableCell>global</TableCell>
          <TableCell><Badge variant="warning" size="sm" dot>Degraded</Badge></TableCell>
          <TableCell align="numeric">3.8M req/s</TableCell>
        </TableRow>
      </TableBody>
      <TableFooter>
        <TableRow>
          <TableCell colSpan={3}>Cluster Summary</TableCell>
          <TableCell align="numeric">3 Nodes Active</TableCell>
        </TableRow>
      </TableFooter>
    </Table>
  );
}
```

---

## Visual Variants

The `Table` component provides built-in visual variants for dense layouts, zebra striping, borders, and sticky headers:

```tsx
// Compact / Dense layout for data-dense dashboards
<Table dense>...</Table>

// Zebra striping for alternating row backgrounds
<Table striped>...</Table>

// Sticky pinned thead while scrolling
<Table stickyHeader>...</Table>

// Fixed table column layout
<Table layout="fixed">...</Table>

// Sticky column pinning (left / right / start / end)
<TableHead pinned="left" pinOffset={0}>Pinned Service</TableHead>
<TableCell pinned="left" pinOffset={0}>Auth Service</TableCell>
```

---

## TanStack Table Integration

Connect `@moul-dev/ui` table primitives with `@tanstack/react-table` for multi-column sorting, row selection, filtering, and pagination:

```tsx
import {
  Table,
  TableHeader,
  TableBody,
  TableRow,
  TableHead,
  TableCell,
  Checkbox,
} from '@moul-dev/ui';
import {
  useTable,
  createColumnHelper,
  createCoreRowModel,
  createSortedRowModel,
  createFilteredRowModel,
  createPaginatedRowModel,
  flexRender,
  type SortingState,
  type RowSelectionState,
} from '@tanstack/react-table';
import { useState, useMemo } from 'react';

interface Service {
  id: string;
  name: string;
  region: string;
  status: string;
  latency: string;
}

const columnHelper = createColumnHelper<Service>();

export default function TanStackTableExample({ data }: { data: Service[] }) {
  const [sorting, setSorting] = useState<SortingState>([]);
  const [rowSelection, setRowSelection] = useState<RowSelectionState>({});

  const columns = useMemo(
    () => [
      columnHelper.display({
        id: 'select',
        header: ({ table }) => (
          <Checkbox
            aria-label="Select all"
            isSelected={table.getIsAllPageRowsSelected()}
            isIndeterminate={table.getIsSomePageRowsSelected()}
            onChange={(val) => table.toggleAllPageRowsSelected(val)}
          />
        ),
        cell: ({ row }) => (
          <Checkbox
            aria-label="Select row"
            isSelected={row.getIsSelected()}
            onChange={(val) => row.toggleSelected(val)}
          />
        ),
      }),
      columnHelper.accessor('name', {
        header: 'Service Name',
        cell: (info) => info.getValue(),
      }),
      columnHelper.accessor('region', {
        header: 'Region',
        cell: (info) => info.getValue(),
      }),
      columnHelper.accessor('status', {
        header: 'Status',
        cell: (info) => info.getValue(),
      }),
      columnHelper.accessor('latency', {
        header: 'Latency',
        cell: (info) => info.getValue(),
      }),
    ],
    []
  );

  const table = useTable({
    data,
    columns,
    state: { sorting, rowSelection },
    onSortingChange: setSorting,
    onRowSelectionChange: setRowSelection,
    getCoreRowModel: createCoreRowModel(),
    getSortedRowModel: createSortedRowModel(),
    getFilteredRowModel: createFilteredRowModel(),
    getPaginationRowModel: createPaginatedRowModel(),
  });

  return (
    <Table stickyHeader>
      <TableHeader>
        {table.getHeaderGroups().map((headerGroup) => (
          <TableRow key={headerGroup.id}>
            {headerGroup.headers.map((header) => (
              <TableHead
                key={header.id}
                sortDirection={header.column.getIsSorted()}
                onSort={
                  header.column.getCanSort()
                    ? header.column.getToggleSortingHandler()
                    : undefined
                }
              >
                {header.isPlaceholder
                  ? null
                  : flexRender(
                      header.column.columnDef.header,
                      header.getContext()
                    )}
              </TableHead>
            ))}
          </TableRow>
        ))}
      </TableHeader>
      <TableBody>
        {table.getRowModel().rows.map((row) => (
          <TableRow key={row.id} selected={row.getIsSelected()}>
            {row.getVisibleCells().map((cell) => (
              <TableCell key={cell.id}>
                {flexRender(cell.column.columnDef.cell, cell.getContext())}
              </TableCell>
            ))}
          </TableRow>
        ))}
      </TableBody>
    </Table>
  );
}
```

---

## Large Dataset Virtualization (TanStack Virtual)

Render thousands of table rows with smooth 60fps windowing using `@tanstack/react-virtual`:

<ComponentPreview>
  <TableVirtualDemo />
</ComponentPreview>

```tsx
import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from '@moul-dev/ui';
import { useVirtualizer } from '@tanstack/react-virtual';
import { useRef } from 'react';

export function VirtualTableExample({ data }: { data: any[] }) {
  const parentRef = useRef<HTMLDivElement>(null);

  const rowVirtualizer = useVirtualizer({
    count: data.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 40,
    overscan: 10,
  });

  const virtualRows = rowVirtualizer.getVirtualItems();
  const totalSize = rowVirtualizer.getTotalSize();
  const paddingTop = virtualRows.length > 0 ? virtualRows[0].start || 0 : 0;
  const paddingBottom =
    virtualRows.length > 0
      ? totalSize - (virtualRows[virtualRows.length - 1].end || 0)
      : 0;

  return (
    <div ref={parentRef} style={{ maxHeight: '360px', overflowY: 'auto' }}>
      <Table stickyHeader dense wrapInContainer={false}>
        <TableHeader>
          <TableRow>
            <TableHead>Hostname</TableHead>
            <TableHead>IP Address</TableHead>
            <TableHead align="numeric">CPU</TableHead>
            <TableHead>Status</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {paddingTop > 0 && (
            <tr>
              <td style={{ height: `${paddingTop}px` }} colSpan={4} />
            </tr>
          )}
          {virtualRows.map((virtualRow) => {
            const item = data[virtualRow.index];
            return (
              <TableRow key={item.id}>
                <TableCell>{item.hostname}</TableCell>
                <TableCell>{item.ip}</TableCell>
                <TableCell align="numeric">{item.cpu}</TableCell>
                <TableCell>{item.status}</TableCell>
              </TableRow>
            );
          })}
          {paddingBottom > 0 && (
            <tr>
              <td style={{ height: `${paddingBottom}px` }} colSpan={4} />
            </tr>
          )}
        </TableBody>
      </Table>
    </div>
  );
}
```

---

## Server-Side Pagination with TanStack Query & Pacer

Handle asynchronous server data fetching with cached background refetches and loading skeletons:

<ComponentPreview>
  <TableQueryDemo />
</ComponentPreview>

```tsx
import {
  Table,
  TableHeader,
  TableBody,
  TableRow,
  TableHead,
  TableCell,
  TableSkeleton,
  Button,
} from '@moul-dev/ui';
import { useQuery } from '@tanstack/react-query';
import { useDebouncedValue } from '@tanstack/react-pacer';
import { useState } from 'react';

export function QueryTableExample() {
  const [page, setPage] = useState(1);
  const [search, setSearch] = useState('');
  const [debouncedSearch] = useDebouncedValue(search, 300);

  const { data, isFetching } = useQuery({
    queryKey: ['cluster-nodes', page, debouncedSearch],
    queryFn: () => fetchNodes({ page, search: debouncedSearch }),
  });

  return (
    <Table dense>
      <TableHeader>
        <TableRow>
          <TableHead>ID</TableHead>
          <TableHead>Node Name</TableHead>
          <TableHead>Cluster</TableHead>
          <TableHead align="numeric">Health Score</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {isFetching && !data ? (
          <TableSkeleton rows={5} columns={4} />
        ) : (
          data?.records.map((node: any) => (
            <TableRow key={node.id}>
              <TableCell>{node.id}</TableCell>
              <TableCell>{node.name}</TableCell>
              <TableCell>{node.cluster}</TableCell>
              <TableCell align="numeric">{node.score}</TableCell>
            </TableRow>
          ))
        )}
      </TableBody>
    </Table>
  );
}
```

---

## Loading and Empty State Components

Display animated skeleton pulse rows while data is fetching, or render custom empty states when queries return 0 results:

```tsx
// 1. Loading Skeleton Rows
<TableBody>
  <TableSkeleton rows={5} columns={4} />
</TableBody>

// 2. Empty State Row
<TableBody>
  <TableEmpty colSpan={4}>
    <EmptyState
      title="No cluster nodes found"
      description="Try clearing active query filters."
    />
  </TableEmpty>
</TableBody>
```

---

## Props Reference

### Table Props

<TypeTable
  type={{
    dense: {
      type: 'boolean',
      default: 'false',
      description: 'Enables compact padding for high-density data tables.',
    },
    striped: {
      type: 'boolean',
      default: 'false',
      description: 'Applies alternating zebra background colors to even rows.',
    },
    hoverable: {
      type: 'boolean',
      default: 'true',
      description: 'Enables subtle hover background highlighting on rows.',
    },
    stickyHeader: {
      type: 'boolean',
      default: 'false',
      description: 'Keeps the thead header pinned to the top on vertical scroll.',
    },
    bordered: {
      type: 'boolean',
      default: 'false',
      description: 'Draws vertical column borders between cells.',
    },
    layout: {
      type: "'auto' | 'fixed'",
      default: "'auto'",
      description: 'Controls the CSS table-layout algorithm.',
    },
    wrapInContainer: {
      type: 'boolean',
      default: 'true',
      description: 'Wraps the table element in a responsive horizontal scroll container.',
    },
    containerStyle: {
      type: 'StyleXStyles',
      description: 'StyleX styles applied to the responsive container wrapper.',
    },
    containerClassName: {
      type: 'string',
      description: 'CSS class name applied to the responsive container wrapper.',
    },
    style: {
      type: 'StyleXStyles',
      description: 'StyleX styles applied to the table element.',
    },
  }}
/>

### TableHeader Props

<TypeTable
  type={{
    sticky: {
      type: 'boolean',
      description: 'Overrides table-level stickyHeader setting for this thead.',
    },
    style: {
      type: 'StyleXStyles',
      description: 'StyleX styles applied to the thead element.',
    },
  }}
/>

### TableRow Props

<TypeTable
  type={{
    selected: {
      type: 'boolean',
      default: 'false',
      description: 'Applies primary selected highlight background and aria-selected="true".',
    },
    hoverable: {
      type: 'boolean',
      description: 'Overrides table-level hoverable setting for this specific row.',
    },
    interactive: {
      type: 'boolean',
      default: 'false',
      description: 'Sets cursor: pointer for clickable rows.',
    },
    style: {
      type: 'StyleXStyles',
      description: 'StyleX styles applied to the tr element.',
    },
  }}
/>

### TableHead Props

<TypeTable
  type={{
    align: {
      type: "'left' | 'center' | 'right' | 'numeric'",
      default: "'left'",
      description: 'Header text alignment. "numeric" aligns right and enables tabular figures.',
    },
    sortDirection: {
      type: "'asc' | 'desc' | 'ascending' | 'descending' | false | null",
      description: 'Sort direction indicator state (displays directional chevron icon).',
    },
    onSort: {
      type: '() => void',
      description: 'Callback fired when sortable column header is clicked or activated via Enter/Space.',
    },
    showSortIndicator: {
      type: 'boolean',
      default: 'true',
      description: 'Whether to render the directional sorting indicator icon.',
    },
    pinned: {
      type: "'left' | 'right' | 'start' | 'end'",
      description: 'Pins the column header to the left (start) or right (end) edge on horizontal scroll.',
    },
    pinOffset: {
      type: 'number | string',
      description: 'Sticky pinning offset (e.g. 0, "120px"). Uses CSS logical properties.',
    },
    style: {
      type: 'StyleXStyles',
      description: 'StyleX styles applied to the th element.',
    },
  }}
/>

### TableCell Props

<TypeTable
  type={{
    align: {
      type: "'left' | 'center' | 'right' | 'numeric'",
      default: "'left'",
      description: 'Cell text alignment. "numeric" aligns right and enables tabular numbers.',
    },
    tabular: {
      type: 'boolean',
      default: 'false',
      description: 'Forces font-variant-numeric: tabular-nums for consistent monospace number alignment.',
    },
    pinned: {
      type: "'left' | 'right' | 'start' | 'end'",
      description: 'Pins the cell to the left (start) or right (end) edge on horizontal scroll.',
    },
    pinOffset: {
      type: 'number | string',
      description: 'Sticky pinning offset (e.g. 0, "120px"). Uses CSS logical properties.',
    },
    style: {
      type: 'StyleXStyles',
      description: 'StyleX styles applied to the td element.',
    },
  }}
/>

### TableEmpty Props

<TypeTable
  type={{
    colSpan: {
      type: 'number',
      default: '1',
      description: 'Number of columns the empty state cell should span.',
    },
    cellStyle: {
      type: 'StyleXStyles',
      description: 'StyleX styles applied to the inner td cell.',
    },
    style: {
      type: 'StyleXStyles',
      description: 'StyleX styles applied to the outer tr element.',
    },
  }}
/>

### TableSkeleton Props

<TypeTable
  type={{
    rows: {
      type: 'number',
      default: '5',
      description: 'Number of skeleton placeholder rows to render.',
    },
    columns: {
      type: 'number',
      default: '4',
      description: 'Number of skeleton placeholder columns per row.',
    },
    style: {
      type: 'StyleXStyles',
      description: 'StyleX styles applied to each skeleton tr element.',
    },
  }}
/>
