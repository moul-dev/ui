---
title: TextArea
description: Documentation for the TextArea component.
---

import { TextArea } from '@moul-dev/ui';


# TextArea

## Preview

<ComponentPreview>
  <TextArea label="Bio" placeholder="Tell us about yourself..." description="Must be under 200 words." className="w-80" />
</ComponentPreview>

The `TextArea` component is built with React Aria and styled using StyleX.

## Import

```tsx
import { TextArea } from '@moul-dev/ui';
```

## Usage

Here is a basic example of how to use the `TextArea` component:

```tsx
import { TextArea } from '@moul-dev/ui';

export default function Example() {
  return (
    <TextArea>
      {/* Component content */}
    </TextArea>
  );
}
```
