---
title: RadioGroup
description: Documentation for the RadioGroup component.
---

import { RadioGroup, Radio } from '@moul-dev/ui';


# RadioGroup

## Preview

<ComponentPreview>
  <RadioGroup label="Subscription Type">
    <Radio value="free">Free</Radio>
    <Radio value="pro">Pro</Radio>
    <Radio value="enterprise">Enterprise</Radio>
  </RadioGroup>
</ComponentPreview>

The `RadioGroup` component is built with React Aria and styled using StyleX.

## Import

```tsx
import { RadioGroup } from '@moul-dev/ui';
```

## Usage

Here is a basic example of how to use the `RadioGroup` component:

```tsx
import { RadioGroup } from '@moul-dev/ui';

export default function Example() {
  return (
    <RadioGroup>
      {/* Component content */}
    </RadioGroup>
  );
}
```

## Props

### RadioGroup Props

<TypeTable
  type={{
    'orientation': {
      type: "'horizontal' | 'vertical'",
      default: "'vertical'",
      description: "The layout orientation of the radio elements.",
    },
    'value': {
      type: "string",
      description: "The value of the selected radio button (controlled).",
    },
    'defaultValue': {
      type: "string",
      description: "The value of the initially selected radio button (uncontrolled).",
    },
    'isDisabled': {
      type: "boolean",
      default: "false",
      description: "Whether the entire radio group is disabled.",
    },
    'isInvalid': {
      type: "boolean",
      description: "Whether the radio group value is invalid.",
    },
  }}
/>

<Accordions>
  <Accordion title="Value">
    <TypeTable
          type={{
            'onChange': {
              type: "(value: string) => void",
              description: "Handler called when the selected value changes.",
            },
          }}
        />
  </Accordion>
  <Accordion title="Styling">
    <TypeTable
          type={{
            'className': {
              type: "ClassNameOrFunction<RadioGroupRenderProps>",
              default: "'react-aria-RadioGroup'",
              description: "The CSS className for the group.",
            },
          }}
        />
  </Accordion>
</Accordions>

Default className: `react-aria-RadioGroup`

| Render Prop | CSS Selector |
| :--- | :--- |
| `isDisabled` <br/> Whether the group is disabled. | `[data-disabled]` |
| `isInvalid` <br/> Whether the group value is invalid. | `[data-invalid]` |

